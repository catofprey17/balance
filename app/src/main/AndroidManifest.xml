<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.c17.balance">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_SMS"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".AboutActivity"
            android:label="@string/pref_about_label" />


        <activity
            android:name=".EditReceiptScreen.EditReceiptActivity"
            android:label="@string/activity_edit_receipt_title"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".DetailScreen.DetailActivity"
            android:label="@string/activity_detail_title" />
        <activity
            android:name=".SettingsScreen.SettingsActivity"
            android:label="@string/activity_settings_title" />
        <activity android:name=".MonthScreen.MonthActivity" />
        <activity
            android:name=".PeriodsScreen.PeriodsActivity"
            android:label="@string/activity_periods_title" />
        <activity android:name=".MainScreen.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>